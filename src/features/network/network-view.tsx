"use client";

import { useState, type Dispatch, type SetStateAction } from "react";
import type { DnsCheck, DnsSettings, DnsStatus, MihomoDnsStatus, NetworkStatus } from "../../shared/types/control-plane";
import { SystemDnsControl } from "./system-dns-control";
// DNS самого VPS управляется отдельным блоком сети через apply_system.

type Tone = "good" | "attention" | "critical";
type Props = { status: NetworkStatus | null; loading: boolean; onRefresh: () => void; dns: DnsStatus | null; dnsDraft: DnsSettings | null; dnsChecks: Record<string, DnsCheck>; checkingDns: boolean; busy: boolean; setDnsDraft: Dispatch<SetStateAction<DnsSettings | null>>; checkDnsProviders: (providerId?: string) => Promise<void> | void; saveDnsSettings: () => Promise<void> | void; mihomoDns: MihomoDnsStatus | null; mihomoDnsDraft: Record<string, string | number | boolean>; mihomoDnsBusy: boolean; setMihomoDnsDraft: Dispatch<SetStateAction<Record<string, string | number | boolean>>>; saveMihomoDns: () => Promise<void> | void };
const scope = [["apply_wg", "WG", "WireGuard"], ["apply_awg", "AWG", "AmneziaWG"], ["apply_shadowsocks", "SS", "Shadowsocks"], ["apply_vrx", "VLESS", "Прямой VLESS"]] as const;
const scopeProtocol = { apply_wg: "wg", apply_awg: "awg", apply_shadowsocks: "shadowsocks", apply_vrx: "vless-reality-xhttp" } as const;
// Контракт применения: «Изменения применяются только к отмеченным каналам». Для VLESS Xray получит выбранные resolver-ы и перезапустится; для SS серверный трафик не изменяется.
const empty = (text?: string | null) => text || "—";
const routeTone = (status: NetworkStatus | null): Tone => status?.route.mode === "direct" ? "good" : status?.route.mode === "proxy_or_cdn" ? "attention" : "critical";
const domainTone = (route: string): Tone => route === "direct" ? "good" : route === "proxy_or_cdn" ? "attention" : "critical";

function Signal({ tone }: { tone: Tone }) { return <i className={`networkSignal ${tone}`} aria-hidden="true" />; }
function Caption({ children }: { children: string }) { return <span className="networkCaption">{children}</span>; }

export function NetworkView({ status, loading, onRefresh, dns, dnsDraft, dnsChecks, checkingDns, busy, setDnsDraft, checkDnsProviders, saveDnsSettings, mihomoDns, mihomoDnsDraft, mihomoDnsBusy, setMihomoDnsDraft, saveMihomoDns }: Props) {
  const [mihomoSelected, setMihomoSelected] = useState(true);
  const tone = routeTone(status);
  if (!status) return <main className="networkBoard"><NetworkHeader status={status} loading={loading} onRefresh={onRefresh} tone={tone} /><div className="networkWaiting"><Signal tone="attention" /><strong>Сетевая конфигурация загружается</strong><span>Получаем домены, маршруты и точки доступа VPS.</span></div></main>;
  const direct = status.domains.filter((item) => item.route === "direct").length;
  const cdn = status.domains.filter((item) => item.route === "proxy_or_cdn").length;
  return <main className="networkBoard">
    <NetworkHeader status={status} loading={loading} onRefresh={onRefresh} tone={tone} />
    <div className="networkIntro"><div><Caption>LIVE NETWORK MAP</Caption><h2>Сеть сервера</h2><p>Текущая схема доступа и правила разрешения имён в одном рабочем пространстве.</p></div><div className="networkCounters"><span><b>{status.domains.length}</b> доменов</span><span><b>{direct}</b> direct</span><span><b>{cdn}</b> CDN</span></div></div>
    <section className="networkBoardGrid" aria-label="Состояние сети">
      <aside className="networkInfoRail"><div className="networkRailTitle"><Caption>SERVER POSTURE</Caption><Signal tone={tone} /></div><strong className={`networkRouteValue ${tone}`}>{status.route.label}</strong><p>{status.route.evidence[0] || "Маршрут не определён"}</p><dl><div><dt>EDGE</dt><dd>{status.edge.provider}<small>{status.edge.mode}</small></dd></div><div><dt>TLS</dt><dd>{status.tls.mode}<small>{status.tls.certificate_source}</small></dd></div><div><dt>RESOLVERS</dt><dd>{status.resolvers.length}<small>{status.resolvers.join(" · ") || "не настроены"}</small></dd></div></dl></aside>
      <div className="networkRouteCanvas"><div className="networkPath" aria-hidden="true" /><div className="networkPathNode"><b>01</b><div><Caption>REQUEST</Caption><strong>Домен ресурса</strong><span>DNS → access point</span></div></div><div className={`networkPathNode selected ${tone}`}><b>02</b><div><Caption>ROUTE</Caption><strong>{status.route.label}</strong><span>{status.edge.provider} · {status.edge.mode}</span></div></div><div className="networkPathNode"><b>03</b><div><Caption>ORIGIN</Caption><strong>{status.domains.length} доменов</strong><span>{direct} direct · {cdn} через CDN</span></div></div></div>
      <aside className="networkAccessRail"><div className="networkRailTitle"><Caption>ACCESS POINTS</Caption><Signal tone={status.access.mode === "protected" ? "good" : "attention"} /></div><div className="networkAccessPrimary"><Caption>ACTIVE ADDRESS</Caption><code>{empty(status.access.panel_url)}</code><b>{status.access.mode}</b></div><div><Caption>DIRECT ORIGIN</Caption><code>{empty(status.access.direct_url)}</code></div><div><Caption>PROTECTED ADDRESS</Caption><code>{empty(status.access.protected_url)}</code></div></aside>
    </section>
    <section className="networkDomains"><header><div><Caption>DOMAIN ROUTES</Caption><h2>Маршруты доменов</h2></div><span>{status.domains.length} записей</span></header><div className="networkDomainHeader"><span>ДОМЕН / РОЛЬ</span><span>RESOLVED ADDRESSES</span><span>MODE</span></div>{status.domains.length ? status.domains.map((domain) => { const itemTone = domainTone(domain.route); return <div className="networkDomain" key={`${domain.role}-${domain.value}`}><div><Signal tone={itemTone} /><strong>{domain.value}</strong><small>{domain.role} · {domain.source}</small></div><code>{domain.resolved.join(", ") || "не разрешается"}</code><b className={itemTone}>{domain.route === "direct" ? "DIRECT" : domain.route === "proxy_or_cdn" ? "CDN" : "ERROR"}</b></div>; }) : <p className="networkNoData">Домены не настроены.</p>}</section>
    <DnsView dns={dns} dnsDraft={dnsDraft} dnsChecks={dnsChecks} checkingDns={checkingDns} busy={busy} setDnsDraft={setDnsDraft} checkDnsProviders={checkDnsProviders} saveDnsSettings={saveDnsSettings} mihomoAvailable={Boolean(mihomoDns)} mihomoSelected={mihomoSelected} onMihomoSelected={setMihomoSelected} />
    <SystemDnsControl dns={dns} dnsDraft={dnsDraft} setDnsDraft={setDnsDraft} />
    {mihomoDns && mihomoSelected && <MihomoCore policy={mihomoDns} draft={mihomoDnsDraft} busy={mihomoDnsBusy} setDraft={setMihomoDnsDraft} onSave={saveMihomoDns} />}
  </main>;
}

function NetworkHeader({ status, loading, onRefresh, tone }: { status: NetworkStatus | null; loading: boolean; onRefresh: () => void; tone: Tone }) { return <header className="networkHeader"><div className="networkHeaderTitle"><span className="networkLogo">↗</span><div><Caption>NETWORK / VPS</Caption><h1>Сеть</h1></div></div><div className={`networkCurrentRoute ${tone}`}><Signal tone={tone} /><div><Caption>ACTIVE ROUTE</Caption><strong>{status?.route.label || "Определяем…"}</strong></div></div><button type="button" className="networkReload" onClick={onRefresh} disabled={loading}>{loading ? "Проверяем…" : "Обновить"}</button></header>; }

export function DnsView({ dns, dnsDraft, dnsChecks, checkingDns, busy, setDnsDraft, checkDnsProviders, saveDnsSettings, mihomoAvailable = false, mihomoSelected = false, onMihomoSelected }: { dns: DnsStatus | null; dnsDraft: DnsSettings | null; dnsChecks: Record<string, DnsCheck>; checkingDns: boolean; busy: boolean; setDnsDraft: Dispatch<SetStateAction<DnsSettings | null>>; checkDnsProviders: (providerId?: string) => Promise<void> | void; saveDnsSettings: () => Promise<void> | void; mihomoAvailable?: boolean; mihomoSelected?: boolean; onMihomoSelected?: (value: boolean) => void }) {
  const [activeScope, setActiveScope] = useState("system");
  const selected = dns?.providers.find((item) => item.id === dnsDraft?.selected_id);
  const activeProfileId = dnsDraft?.profiles?.[activeScope] || dnsDraft?.selected_id;
  const activeSelected = dns?.providers.find((item) => item.id === activeProfileId);
  const selectedName = activeSelected?.name || selected?.name || dnsDraft?.custom?.name || "DNS не выбран";
  const effects = Object.entries(dns?.protocol_effect_details || {}).filter(([, item]) => item.installed);
  const available = (key: typeof scope[number][0]) => Boolean(dns?.protocol_effect_details?.[scopeProtocol[key]]?.installed);
  const effectScope = (name: string) => name === "server_xray" ? "СРАЗУ" : name === "new_profiles" ? "НОВЫЕ КОНФИГИ" : "РЕКОМЕНДАЦИЯ";
  const choose = (id: string) => setDnsDraft((current) => current ? { ...current, selected_id: activeScope === "system" ? id : current.selected_id, profiles: { ...(current.profiles || {}), [activeScope]: id } } : current);
  return <section className="networkDnsBoard" aria-label="SECURE CHANNELS · DNS"><header className="networkDnsTitle"><div><Caption>DNS POLICY</Caption><h2>Разрешение имён</h2><p>Единые настройки DNS для защищённых каналов и новых конфигураций.</p></div><div className="networkDnsSelected"><Caption>SELECTED PROFILE</Caption><strong>{selectedName}</strong></div></header><div className="networkDnsMain"><article className="networkResolverCatalog"><header><div><Caption>RESOLVERS</Caption><h3>Каталог DNS</h3></div><button type="button" onClick={() => void checkDnsProviders()} disabled={checkingDns}>{checkingDns ? "Проверяем…" : "Проверить все"}</button></header><div>{(dns?.providers || []).map((provider) => { const check = dnsChecks[provider.id]; const active = dnsDraft?.selected_id === provider.id; return <button type="button" className={`networkResolver ${active ? "active" : ""}`} key={provider.id} onClick={() => choose(provider.id)}><span>{provider.country}</span><div><strong>{provider.name}</strong><small>{provider.filter}</small><code>{provider.addresses.join("  ")}</code></div><aside><b className={check?.udp_ok ? "ok" : ""}>UDP {check?.udp_ms != null ? `${check.udp_ms} ms` : "—"}</b><b className={check?.doh_ok ? "ok" : ""}>DoH {check?.doh_ms != null ? `${check.doh_ms} ms` : provider.doh_url ? "—" : "нет"}</b></aside><em>{active ? "ВЫБРАН" : "ВЫБРАТЬ"}</em></button>; })}</div></article><aside className="networkScope"><header><Caption>APPLICATION SCOPE</Caption><h3>Куда применять</h3></header><div>{scope.map(([key, code, title]) => { const enabled = available(key); return <label className={`${dnsDraft?.[key] && enabled ? "active" : ""} ${enabled ? "" : "disabled"}`} key={key}><input type="checkbox" disabled={!enabled} checked={enabled && (dnsDraft?.[key] ?? true)} onChange={(event) => setDnsDraft((current) => current ? { ...current, [key]: event.target.checked } : current)} /><b>{code}</b><span><strong>{title}</strong><small>{enabled ? key === "apply_vrx" ? "Применяется сразу" : "Только новые конфиги клиентов" : "Протокол не установлен"}</small></span></label>; })}{onMihomoSelected && <label className={`${mihomoSelected && mihomoAvailable ? "active" : ""} ${mihomoAvailable ? "" : "disabled"}`}><input type="checkbox" disabled={!mihomoAvailable} checked={mihomoAvailable && mihomoSelected} onChange={(event) => onMihomoSelected(event.target.checked)} /><b>MI</b><span><strong>Mihomo</strong><small>{mihomoAvailable ? "Настройки ядра ниже" : "Mihomo не установлен"}</small></span></label>}</div><div className="networkDnsOptions"><label><span><strong>Резервный DNS</strong><small>Использовать второй адрес</small></span><input type="checkbox" checked={dnsDraft?.fallback_enabled ?? true} onChange={(event) => setDnsDraft((current) => current ? { ...current, fallback_enabled: event.target.checked } : current)} /></label><label className={dns?.protocol_effect_details?.["vless-reality-xhttp"]?.installed ? "" : "disabled"}><span><strong>DoH для VLESS</strong><small>Зашифрованный resolver для Xray</small></span><input type="checkbox" disabled={!dns?.protocol_effect_details?.["vless-reality-xhttp"]?.installed} checked={dnsDraft?.prefer_encrypted ?? false} onChange={(event) => setDnsDraft((current) => current ? { ...current, prefer_encrypted: event.target.checked } : current)} /></label></div></aside></div><div className="networkDnsExtras"><article><header><div><Caption>EXTERNAL RESOLVER</Caption><h3>Сторонний DNS</h3><p>Добавьте адрес, которого нет в каталоге.</p></div><button type="button" className={dnsDraft?.selected_id === "custom" ? "active" : ""} onClick={() => setDnsDraft((current) => current ? { ...current, selected_id: "custom", custom: current.custom || { name: "Сторонний DNS", addresses: [""], doh_url: "" } } : current)}>{dnsDraft?.selected_id === "custom" ? "Выбран" : "Добавить"}</button></header>{dnsDraft?.selected_id === "custom" && <div className="networkCustomFields"><label>Название<input value={dnsDraft.custom?.name || ""} onChange={(event) => setDnsDraft((current) => current ? { ...current, custom: { ...(current.custom || { addresses: [""], doh_url: "" }), name: event.target.value } } : current)} /></label><label>IP-адреса<input value={(dnsDraft.custom?.addresses || []).join(", ")} onChange={(event) => setDnsDraft((current) => current ? { ...current, custom: { ...(current.custom || { name: "DNS", doh_url: "" }), addresses: event.target.value.split(",").map((item) => item.trim()).filter(Boolean) } } : current)} /></label><label>DoH URL<input value={dnsDraft.custom?.doh_url || ""} onChange={(event) => setDnsDraft((current) => current ? { ...current, custom: { ...(current.custom || { name: "DNS", addresses: [""] }), doh_url: event.target.value } } : current)} /></label></div>}</article><article><header><div><Caption>EFFECTIVE STATE</Caption><h3>Фактическое применение</h3><p>Текущее состояние установленных протоколов.</p></div><span>{effects.length ? `${effects.length} протокола` : "Нет установленных протоколов"}</span></header><div className="networkEffectiveRows">{effects.length ? effects.map(([protocol, effect]) => <div className={effect.matches_selected ? "same" : "different"} key={protocol}><b>{protocol === "vless-reality-xhttp" ? "VLESS" : protocol === "shadowsocks" ? "SS" : protocol.toUpperCase()}</b><code>{effect.value}</code><small>{effectScope(effect.scope)} · {effect.matches_selected ? "СОВПАДАЕТ" : "ОТЛИЧАЕТСЯ"}</small></div>) : <p>После установки протокола появится его фактический DNS.</p>}</div></article></div><aside className="networkDnsImpact"><strong>После применения</strong><span><b>VLESS</b> перезапустит Xray с новым resolver.</span><span><b>WG/AWG</b> получат DNS в новых конфигурациях.</span><span><b>SS</b> не изменит серверный трафик.</span></aside><footer className="networkApply"><div><Caption>READY TO APPLY</Caption><strong>{selectedName}</strong><small>Изменения применяются только к отмеченным каналам.</small></div><button type="button" onClick={() => void saveDnsSettings()} disabled={busy || !dnsDraft}>{busy ? "Применяем…" : "Применить политику"}</button></footer></section>;
}

function MihomoCore({ policy, draft, busy, setDraft, onSave }: { policy: MihomoDnsStatus; draft: Record<string, string | number | boolean>; busy: boolean; setDraft: Dispatch<SetStateAction<Record<string, string | number | boolean>>>; onSave: () => Promise<void> | void }) { const field = (key: string) => policy.schema.find((item) => item.key === key); const update = (key: string, value: string | number | boolean) => setDraft((current) => ({ ...current, [key]: value })); const mode = String(draft.enhanced_mode || "fake-ip"); const name = (key: string) => { const selected = String(draft[key] || ""); const option = field(key)?.options?.find((item) => typeof item === "string" ? item === selected : item.value === selected); return typeof option === "string" ? option : option?.label || selected; }; return <section className="networkCore"><header><div><Caption>MIHOMO CORE</Caption><h2>DNS-обработка ядра</h2><p>Отдельный профиль Mihomo, согласованный с общей DNS-политикой.</p></div><button type="button" onClick={() => void onSave()} disabled={busy}>{busy ? "Сохраняем…" : "Сохранить ядро"}</button></header><div className="networkCoreSummary"><span><Caption>MODE</Caption><strong>{mode === "fake-ip" ? "Fake IP" : "Redir host"}</strong></span><span><Caption>NAMESERVER</Caption><strong>{name("nameserver")}</strong></span><span><Caption>FALLBACK</Caption><strong>{name("fallback")}</strong></span></div><div className="networkCoreGrid"><article><header><b>Режим обработки</b><small>Ответы для приложений</small></header><div className="networkModeButtons">{(field("enhanced_mode")?.options || ["fake-ip", "redir-host"]).map((option) => { const value = typeof option === "string" ? option : option.value; return <button type="button" className={mode === value ? "active" : ""} key={value} onClick={() => update("enhanced_mode", value)}><strong>{value === "fake-ip" ? "Fake IP" : "Redir host"}</strong><small>{value === "fake-ip" ? "Быстрый режим" : "Совместимый режим"}</small></button>; })}</div></article><article><header><b>Дополнительная обработка</b><small>Опции DNS-запросов</small></header><div className="networkCoreOptions"><label>IPv6<input type="checkbox" checked={Boolean(draft.ipv6)} onChange={(event) => update("ipv6", event.target.checked)} /></label><label>HTTP/3 для DoH<input type="checkbox" checked={Boolean(draft.prefer_h3)} onChange={(event) => update("prefer_h3", event.target.checked)} /></label><label>Алгоритм кэша<select value={String(draft.cache_algorithm || "lru")} onChange={(event) => update("cache_algorithm", event.target.value)}>{(field("cache_algorithm")?.options || ["lru", "arc"]).map((option) => { const value = typeof option === "string" ? option : option.value; return <option key={value} value={value}>{typeof option === "string" ? value.toUpperCase() : option.label}</option>; })}</select></label></div></article><article className="networkCoreResolver"><header><b>Резолверы ядра</b><small>Порядок обращения Mihomo к DNS</small></header><div>{(["nameserver", "fallback"] as const).map((key) => <label key={key}><span>{key === "nameserver" ? "Основной DNS" : "Резервный DNS"}</span><select value={String(draft[key] || "")} onChange={(event) => update(key, event.target.value)}>{(field(key)?.options || []).map((option) => { const value = typeof option === "string" ? option : option.value; return <option key={value} value={value}>{typeof option === "string" ? value : option.label}</option>; })}</select></label>)}</div></article></div>{mode === "fake-ip" && <label className="networkFakeIp"><span>Исключения Fake IP<small>Домены будут разрешаться напрямую</small></span><textarea rows={2} value={String(draft.fake_ip_filter || "")} onChange={(event) => update("fake_ip_filter", event.target.value)} /></label>}</section>; }
